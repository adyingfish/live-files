# 宿主机上 openclaw 的 workspace 目录(会被映射到容器内 /workspace)
# 容器内: /workspace → workspace root, /workspace/skills → skills root
OPENCLAW_WORKSPACE=/opt/1panel/apps/openclaw/OpenClaw/data/workspace

# 部署时复制为 .env 并根据实际情况修改:
# cp .env.example .env
