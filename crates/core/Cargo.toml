[package]
name = "live-files-core"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
globset = "0.4"
notify = "6"
notify-debouncer-full = "0.3"
tokio = { version = "1", features = ["sync"] }

[dev-dependencies]
tempfile = "3"
